# ── Build artifacts ───────────────────────────────────────────────

# Final disk image
os.img
boot.img

# Generated filesystem include
fs.inc
build/fs.inc

# Compiled userland binaries
bin/

# Intermediate build files
kernel.bin
bootloader.bin
kernel.lst

# ── Copied build scripts / sources (source of truth is build/, kernel/, boot/) ──

# Copied include files (source of truth is kernel/)

# ── Content directories (populated from kernel/, build/ at build time) ──
etc/
proc/
var/
dev/
usr/
lib/

# ── Editor / OS noise ─────────────────────────────────────────────
*~
*.swp
*.swo
.vscode/
.idea/
.DS_Store
Thumbs.db
.github/
.hermes/
scripts/
build/code-index/
Elite (video game) - Wikipedia.pdf
build/*.img

# ── AI session handoffs (local-only, regenerated each session) ───────────
.github/ai-context/
build/*.png
build/*.ppm

